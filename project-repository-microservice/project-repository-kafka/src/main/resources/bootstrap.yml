spring:
  profiles:
    active: dev
  application:
    name: kafka-study

# 暴露端点
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
